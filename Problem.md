이번 C 전환 과정에서 실제로 발생한 문제는 크게 세 가지였습니다.
절대주소 포인터 변환 실패
커널의 demand paging 테스트에는 다음과 같은 코드가 필요합니다.
volatile uint64_t *address =
    (volatile uint64_t *)0x0000007FFFFFE000;
Clang은 이를 LLVM IR에서 대략 다음 형태로 변환합니다.
ptr inttoptr (i64 549755805696 to ptr)
기존 cvmir는 심볼 주소나 NULL 포인터는 처리했지만, 정수 상수를 포인터로 바꾸는 constant expression은 처리하지 못했습니다.
unsupported LLVM constant pointer expression
해결 방법은 constant_address()가 정수 상수도 절대주소로 받아들이도록 확장하는 것이었습니다. 현재는 이 형태를 MOVI64 절대주소로 정상 변환하고 회귀 테스트도 추가했습니다.
커널이 부트로더 메모리 영역을 침범
기존 어셈블리 커널은 약 9.8 KiB였지만, C 전환 후 커널 이미지는 약 29 KiB가 됐습니다. 현재 LLVM bridge는 -O0이고 SSA 값을 대부분 스택에 저장하기 때문에 생성 코드가 큽니다.
여기에 기존 32 KiB 커널 스택을 붙이자 커널 메모리 끝이 다음처럼 됐습니다.
kernel end:    0x21080
allowed limit: 0x20000
0x20000부터는 2차 부트로더가 사용하는 영역이므로, 그대로 부팅하면 로더와 커널이 겹칩니다. 그래서 로더가 커널을 거부했습니다.
현재는 bootstrap stack을 32 KiB에서 24 KiB로 줄여 다음 위치에서 끝납니다.
kernel end: 0x1F080
또한 [build.ps1 (line 379)](/D:/programing/codingtest/computer/build.ps1:379)이 kernel_bss_end를 확인하여 0x20000을 넘으면 빌드 자체를 실패시킵니다.
다만 현재 남은 여유는 약 0xF80, 즉 3,968바이트뿐입니다. 커널을 계속 확장하려면 장기적으로는 다음 중 하나가 필요합니다.
부트로더를 더 높은 주소로 옮기기
커널 적재 가능 범위를 확장하기
bootstrap 이후 부트로더 메모리를 회수하기
LLVM 최적화를 활성화해 코드 크기 줄이기
예외 handler 호출 시 스택 정렬 문제
CVM ABI에서는 CALLREL 직전 SP가 16바이트 정렬되어야 합니다.
예외 wrapper는 모든 범용 레지스터 15개를 저장합니다.
15 × 8 = 120바이트
일반 C 코드에서 fault가 발생하면 정렬 계산이 맞았지만, 보호 검사에 사용하는 짧은 어셈블리 함수는 함수 진입 직후라 SP % 16 == 8인 상태였습니다. 이 상태에서 CPU 예외 프레임과 15개 레지스터, 추가 padding을 적용하면 오히려 C handler 호출 직전 SP가 8바이트 어긋났습니다.
VM은 비정렬 스택을 허용해서 겉으로는 정상 부팅됐지만 ABI 위반이었습니다. 향후 16바이트 자료형이나 SIMD stack 값이 사용되면 문제가 될 수 있습니다.
그래서 fault probe가 실행 전에 8바이트 로컬 프레임을 먼저 확보하도록 수정했습니다.
ADDI32 SP, -8
이제 모든 probe에서 fault 발생 시점과 C handler 호출 시점 모두 요구 정렬을 만족합니다.
추가로 발견한 구조적 제한
현재 C 커널은 정상 작동하지만 다음 제한은 남아 있습니다.
-O0이라 코드 크기가 크고 실행 속도가 느림
switch, 간접 함수 호출 등 일부 LLVM IR은 아직 미지원
예외 진입과 IRET은 순수 C로 작성할 수 없어 어셈블리가 필요
커널 적재 영역이 0x20000 아래로 제한되어 확장 여유가 작음
현재 PMM은 단순 free-list라 중복 반환이나 잘못된 페이지 반환을 검사하지 않음
즉 C 전환 자체는 성공했지만, 다음 커널 기능을 추가하기 전에 가장 먼저 해결할 구조적 문제는 커널 적재 공간 확장입니다.